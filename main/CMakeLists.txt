# Delays bloqueantes similar Arduino
idf_component_register(SRCS "blink_blocking.c"
                    INCLUDE_DIRS ".")

# Delays con timer
# idf_component_register(SRCS "blink_blocking.c"
#                     INCLUDE_DIRS ".")